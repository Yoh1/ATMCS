{{ form_start(form) }}

    {{ form_row(form.q) }}

    <h4 class>Prix</h4>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.min) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.max) }}
        </div>
    </div>

{{ form_end(form) }}